# Native Tests
This plugin is intended to be ran inside a running CS2 server running a version of CS# and runs tests against the exposed `NativeAPI` methods.

Maintainers can request the non-benchmark suite on a PR with `/smoke-test`.
See [PR smoke tests](../../eng/SMOKE-TESTS.md) for setup, results and server requirements.
